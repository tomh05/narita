<p id="notice"><%= notice %></p>

<h1>Listing Apps</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Event time</th>
      <th>App name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @apps.each do |app| %>
      <tr>
        <td><%= app.event_time %></td>
        <td><%= app.app_name %></td>
        <td><%= link_to 'Destroy', app, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-mini btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>